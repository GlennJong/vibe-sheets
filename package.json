{
  "name": "vibe-sheets-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "app",
    "src"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=app",
    "build": "npm run build --workspaces",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
